name: CI calculator
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  USER: kpk666
  TOKEN: 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
  
jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build and test
      run: |
        # Здесь можете провести тестирование вашего кода, если необходимо
        echo "Testing code"
        
    - name: Build Docker image
      working-directory: Calculator
      run: |
        docker build -t ghcr.io/kpk666/web-calculator:latest .
        echo $DOCKER_PASSWORD | docker login ghcr.io -u kpk666 --password-stdin
        docker push ghcr.io/kpk666/web-calculator:latest

  deploy:
    needs: build
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy with Docker Compose
      working-directory: deploy
      run: |
        docker-compose pull
        docker-compose up -d --force-recreate

  
